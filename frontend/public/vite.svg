<svg xmlns="http://www.w3.org/2000/svg" width="320" height="320" viewBox="0 0 320 320" role="img" aria-labelledby="title desc">
  <title id="title">Catering Booking Mark</title>
  <desc id="desc">Bold modern sharp icon combining a calendar and serving cloche with a check mark.</desc>
  <style>
    :root { --primary:#0F0F0F; --accent:#00E0A4; }
    .stroke { stroke: var(--primary); stroke-width:14; stroke-linecap:square; stroke-linejoin:miter; fill:none; }
    .fill-primary { fill: var(--primary); }
    .fill-accent { fill: var(--accent); }
  </style>

  <!-- Calendar body -->
  <rect x="36" y="56" width="248" height="208" rx="10" class="stroke"/>
  <!-- Calendar header bar -->
  <rect x="36" y="56" width="248" height="48" class="fill-primary"/>

  <!-- Binding tabs -->
  <rect x="86" y="36" width="24" height="32" class="fill-primary"/>
  <rect x="210" y="36" width="24" height="32" class="fill-primary"/>

  <!-- Cloche (serving dome) -->
  <!-- handle -->
  <rect x="154" y="118" width="12" height="12" class="fill-accent"/>
  <!-- dome -->
  <path d="M92 178 A68 68 0 0 1 228 178" class="stroke"/>
  <!-- plate -->
  <line x1="80" y1="196" x2="240" y2="196" class="stroke"/>

  <!-- Confirmation check -->
  <path d="M122 186 L150 212 L206 156" class="stroke"/>

  <!-- Optional brand block (editable wordmark). Delete if not needed. -->
  <rect x="64" y="236" width="192" height="36" rx="6" class="fill-accent"/>
  <text x="160" y="262" text-anchor="middle" font-size="18" font-family="system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Arial, sans-serif" fill="#0B0B0B" font-weight="700" letter-spacing="1">
    CATER • BOOK
  </text>
</svg>
